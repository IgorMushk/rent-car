import React from "react";
import { Menu } from "../Menu/Menu";
import { StyleHeader } from "./Header.styled";

export const Header = () => {
  return (
    <StyleHeader>
      <Menu />
    </StyleHeader>
  );
};
